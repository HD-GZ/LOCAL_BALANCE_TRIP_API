<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProperty scope="context" name="SLACK_WEBHOOK_URI" source="slack.logging.error"/>

    <property name="consoleLogPattern"
        value="%clr([%d{yyyy-MM-dd'T'HH:mm:ss.SSS}]){faint} %clr(%-5level) %clr([%32.32thread]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %msg%n%ex"/>
    <property name="slackLogPattern"
        value=":rotating_light: *LOCAL_BALANCE_TRIP_API ERROR*%n*Time:* `%d{yyyy-MM-dd HH:mm:ss.SSS}`%n*Level:* `%level`%n*Logger:* `%logger{40}`%n*Thread:* `%thread`%n*Request ID:* `%X{requestId:-N/A}`%n*Request:* `%X{httpMethod:-N/A} %X{requestPath:-N/A}`%n*Message:*%n```%.2000msg```%n*Exception:*%n```%ex{20}```"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${consoleLogPattern}</pattern>
        </encoder>
    </appender>

    <appender name="SLACK" class="com.github.maricn.logback.SlackAppender">
        <webhookUri>${SLACK_WEBHOOK_URI}</webhookUri>
        <layout class="ch.qos.logback.classic.PatternLayout">
            <pattern>${slackLogPattern}</pattern>
        </layout>
        <colorCoding>true</colorCoding>
    </appender>

    <appender name="ASYNC_SLACK" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="SLACK"/>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_SLACK"/>
    </root>
</configuration>
